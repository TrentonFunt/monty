#include "monty.h"

/**
  * fnop - No operation, does nothing.
  * @stack_head: Pointer to the head of the stack.
  * @stack_value: Line number (unused).
  */
void fnop(stack_t **stack_head, unsigned int stack_value)
{
	(void) stack_value;
	(void) stack_head;
}
